<%- include('./../_partials/header') %>
    <a href="/myrecs/new">New Search</a>
    <% records.results.forEach(function (result) { %>
    <ul>
      <li>
        <a href="https://www.discogs.com<%= result.uri %>">link</a>
        <%= result.title %>
        <img src="<%= result.thumb %>">
      </li> 
  </ul>
  <% }) %>
</body>
</html>
